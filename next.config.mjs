/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['fonts.googleapis.com', 'storage.example.com', 'images.unsplash.com', 'www.w3.org'],
      },
    };
    
    export default nextConfig;